<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_frame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="in.ac.miet.decoders.pharmapedia.MainActivity"
    tools:showIn="@layout/app_bar_main">


    <ImageView
        android:id="@+id/search_icon"
        android:layout_marginTop="37dp"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="15dp"
        android:src="@drawable/search_icon"/>

    <EditText
        android:id="@+id/search_box"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="37dp"
        android:layout_marginEnd="15dp"
        android:hint="@string/search_hint"
        android:layout_toEndOf="@id/search_icon"
        android:contentDescription="@string/search_content_description"/>

    <Button
        android:id="@+id/search_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_box"
        android:layout_centerHorizontal="true"
        android:text="@string/search_button_name"/>
    <Button
        android:id="@+id/drugs_list_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_button"
        android:layout_marginStart="15dp"
        android:layout_marginTop="20dp"
        android:text="@string/drugs_list_link"/>

    <android.support.v7.widget.AppCompatSpinner
        android:id="@+id/spinner_blacklist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_button"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="15dp"
        android:layout_alignParentRight="true"
        />
</RelativeLayout>

